package week01;
public class UsefulMethods {
    // driver.findElement(By.xpath("(//input[@type='email'])[1]")).sendKeys("huseyinkarabas@gmail.com");
    // driver.findElement(By.xpath("//*[@type='password']")).sendKeys("12345");
    /*
    static WebDriver driver;

    public static void logInButtonClick(){
        WebElement loginButton =driver.findElement(By.xpath("//a[text()=' Signup / Login']"));
        loginButton.click();
    }
    public void userNameTextBoxSendKey(String userName){
        WebElement userNameTextBox=driver.findElement(By.xpath("(//input[@type='email'])[1]"));
        userNameTextBox.sendKeys(userName);
    }
    public static void passwordTextBoxSendKey(String password){
        WebElement passwordTextBox=driver.findElement(By.xpath("//*[@type='password']"));
        passwordTextBox.sendKeys(password);
    }

     */

}
